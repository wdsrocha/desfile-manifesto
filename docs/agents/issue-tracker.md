# Issue Tracker

Issues for this repo live in **Linear**, in the **Trama** project:
https://linear.app/wdsrocha/project/trama-5b3b9f144a14/

Use the `mcp__claude_ai_Linear__*` tools (available in Claude Code) to create, update,
and query issues. The team key is `SOLO`.

## Notes

- `specs/` contains written PRD/spec documents — it is not a task tracker.
  Link specs to Linear issues when relevant, but do not create tasks there.
- When creating issues, assign them to the Trama project (`5babac43-a7e1-4f95-9c73-3aaa0093e403`).
